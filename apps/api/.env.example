# 한국투자증권(KIS) Open API — 모의투자 기준
# 키 발급 후 이 파일을 .env 로 복사해 채우세요. (.env 는 커밋 금지)
# 미설정 시 서버는 stub(가상) 시세로 동작합니다.

KIS_ENV=mock            # mock(모의) | real(실전)
KIS_APP_KEY=
KIS_APP_SECRET=
# PORT=4000
